# ====================================================================
# GPU Server Manager 配置文件
# ====================================================================
#
# ⚠️ 重要安全提示：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 修改所有标记为 [必须修改] 的配置项
# 3. 确保 .env 文件权限为 600：chmod 600 .env
# 4. 不要将 .env 文件提交到 Git
#
# ====================================================================

# --------------------------------------------------------------------
# Flask 基础配置
# --------------------------------------------------------------------

# 运行环境：development（开发） 或 production（生产）
FLASK_ENV=development

# [必须修改] Flask Secret Key - 用于加密Session
# ⚠️ 使用默认值会导致严重安全风险！Session可被伪造
# 生成命令: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-this

# Flask 监听端口
FLASK_PORT=5000

# Flask 监听地址
# 127.0.0.1 - 仅本机访问（最安全）
# 0.0.0.0   - 所有网络接口（需配合防火墙）
FLASK_HOST=127.0.0.1

# --------------------------------------------------------------------
# CORS 跨域配置（安全）
# --------------------------------------------------------------------

# 允许的访问来源（逗号分隔）
# 默认仅允许本地访问，如需其他域名请明确添加
CORS_ORIGINS=http://localhost:5000,http://127.0.0.1:5000
# 生产环境示例：
# CORS_ORIGINS=https://gpu-manager.yourdomain.com

# --------------------------------------------------------------------
# 管理员认证配置
# --------------------------------------------------------------------

ADMIN_USERNAME=admin

# [必须修改] 管理员密码
# ⚠️ 默认密码极其不安全，必须修改为强密码！
# 强密码要求：
# - 至少16个字符
# - 包含大小写字母、数字和特殊字符
# - 不包含常见单词
# 示例: MyStr0ng!P@ssw0rd#2024
ADMIN_PASSWORD=PLEASE_CHANGE_THIS_PASSWORD

# --------------------------------------------------------------------
# 邮件通知配置（可选）
# --------------------------------------------------------------------

# SMTP 服务器配置
# 常用配置：
# - QQ邮箱：smtp.qq.com, 端口587（推荐）或465
# - 163邮箱：smtp.163.com, 端口465
# - Gmail：smtp.gmail.com, 端口587
# - Outlook：smtp.office365.com, 端口587
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# 发件邮箱地址
SMTP_USERNAME=your-email@gmail.com

# 邮箱授权码（不是登录密码！）
# QQ邮箱：设置 → 账户 → POP3/SMTP服务 → 生成授权码
# Gmail：开启两步验证 → 应用专用密码
SMTP_PASSWORD=your-app-password

# 发件人显示地址
SMTP_FROM=your-email@gmail.com

# 是否使用TLS加密
SMTP_USE_TLS=True

# --------------------------------------------------------------------
# 可选：数据库配置
# --------------------------------------------------------------------

# SQLite数据库路径（用于存储任务历史等）
# DATABASE_URL=sqlite:///data/app.db

# --------------------------------------------------------------------
# 配置完成后的检查清单
# --------------------------------------------------------------------
#
# [ ] 已修改 ADMIN_PASSWORD 为强密码
# [ ] 已生成并设置 SECRET_KEY
# [ ] 已配置 FLASK_HOST（推荐 127.0.0.1）
# [ ] 已设置 .env 文件权限为 600
# [ ] 如需邮件通知，已配置 SMTP 信息
# [ ] 如需HTTPS，已生成 SSL 证书
# [ ] 已阅读 SECURITY.md 安全指南
#
# 启动命令：
# - HTTP模式：python run.py --host 127.0.0.1
# - HTTPS模式：python run.py --https --host 127.0.0.1
#
# ====================================================================
